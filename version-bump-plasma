#!/usr/bin/env bash
# SPDX-License-Identifier: GPL-2.0-only
# version-bump-plasma - Bump version for all Plasma repos and create tags.
#
# Plasma repos use set(PROJECT_VERSION "X.Y.Z") in CMakeLists.txt.
# The new version is passed as the first argument (X.Y.Z format).
#
# Unlike KDE's release scripts, no second "bump to next dev version" commit
# is created.  We make patch releases against these tagged versions.
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "$SCRIPT_DIR/scripts/version_bump.py" plasma "$@"
